[pytest]
testpaths = tests/unit
pythonpath = src
addopts = -ra --strict-markers
markers =
    requires_root: integration test that needs to run under root
    requires_vault: integration test that needs /tmp/veracage-test.vc to exist
    requires_wayland: integration test that needs a Wayland session
